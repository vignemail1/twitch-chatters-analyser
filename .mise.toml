# Configuration mise (https://mise.jdx.dev)
# Gestion des outils et variables d'environnement du projet

[tools]
# Versions des outils de développement
go = "1.25"
node = "22"
python = "3.12"

[env]
# Variables techniques (pas de secrets)
COMPOSE_PROJECT_NAME = "twitch-chatters-analyser"
DOCKER_BUILDKIT = "1"
COMPOSE_DOCKER_CLI_BUILD = "1"

# Chargement du .env selon le profil
_.file = [
  ".env",
  { path = ".env.local", optional = true },
]

_.path = [
  "./bin",
  "./scripts"
]

# ===========================================
# PROFIL DEVELOPMENT (par défaut)
# ===========================================
[env.development]
# Charger .env.development en priorité
_.file = [
  ".env.development",
  { path = ".env.development.local", optional = true },
]

APP_ENV = "development"
LOG_LEVEL = "DEBUG"

# Domaines de développement
TWITCH_REDIRECT_URL = "https://twitch-chatters-dev.vignemail1.eu/auth/callback"

# Monitoring désactivé en dev par défaut
ENABLE_MONITORING = "false"

# Redis visible en local pour debug
REDIS_PORT = "6379"

# MySQL visible en local pour debug
MYSQL_PORT = "3306"

# ===========================================
# PROFIL PRODUCTION
# ===========================================
[env.production]
# Charger .env.production
_.file = [
  ".env.production",
  { path = ".env.production.local", optional = true },
]

APP_ENV = "production"
LOG_LEVEL = "INFO"

# Domaines de production
TWITCH_REDIRECT_URL = "https://twitch-chatters.vignemail1.eu/auth/callback"

# Monitoring activé en production
ENABLE_MONITORING = "true"

# Ports non exposés en production
REDIS_PORT = ""
MYSQL_PORT = ""

# ===========================================
# PROFIL STAGING
# ===========================================
[env.staging]
_.file = [
  ".env.staging",
  { path = ".env.staging.local", optional = true },
]

APP_ENV = "staging"
LOG_LEVEL = "INFO"

TWITCH_REDIRECT_URL = "https://twitch-chatters-staging.vignemail1.eu/auth/callback"

ENABLE_MONITORING = "true"

# ===========================================
# TÂCHES
# ===========================================

[tasks.install]
description = "Installer les dépendances Go"
run = "go mod download"

[tasks.build]
description = "Builder les images Docker"
run = "docker-compose build"

[tasks."build:nocache"]
description = "Builder les images Docker sans cache"
run = "docker-compose build --no-cache"

[tasks.up]
description = "Démarrer tous les services (utilise le profil actif)"
run = """
#!/usr/bin/env bash
set -e

if [ "$ENABLE_MONITORING" = "true" ]; then
  echo "📊 Démarrage avec monitoring ($APP_ENV)"
  docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d
else
  echo "🚀 Démarrage sans monitoring ($APP_ENV)"
  docker-compose up -d
fi
"""

[tasks."up:dev"]
description = "Démarrer en mode développement (ports exposés, 1 replica)"
run = "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d"

[tasks."up:prod"]
description = "Démarrer en mode production (monitoring inclus)"
env = { MISE_ENV = "production" }
run = "docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d"

[tasks.down]
description = "Arrêter tous les services"
run = "docker-compose down"

[tasks."down:volumes"]
description = "Arrêter et supprimer les volumes (ATTENTION: perte de données)"
run = "docker-compose down -v"

[tasks.restart]
description = "Redémarrer tous les services"
run = """
#!/usr/bin/env bash
mise run down
mise run up
"""

[tasks.logs]
description = "Afficher les logs de tous les services"
run = "docker-compose logs -f"

[tasks."logs:gateway"]
description = "Afficher les logs du gateway"
run = "docker-compose logs -f gateway"

[tasks."logs:worker"]
description = "Afficher les logs du worker"
run = "docker-compose logs -f worker"

[tasks."logs:db"]
description = "Afficher les logs de la base de données"
run = "docker-compose logs -f db"

[tasks.ps]
description = "Afficher l'état des services"
run = "docker-compose ps"

[tasks.test]
description = "Lancer les tests"
run = "go test -v ./..."

[tasks."test:coverage"]
description = "Lancer les tests avec couverture"
run = "go test -cover -coverprofile=coverage.out ./..."

[tasks.lint]
description = "Linter le code Go"
run = "golangci-lint run"

[tasks."lint:fix"]
description = "Linter et corriger automatiquement"
run = "golangci-lint run --fix"

[tasks.env-check]
description = "Vérifier que toutes les variables d'environnement sont définies"
run = """
#!/usr/bin/env bash
set -e

echo "🔍 Vérification des variables pour l'environnement: $APP_ENV"
echo ""

REQUIRED_VARS=(
  "APP_ENV"
  "TWITCH_CLIENT_ID"
  "TWITCH_CLIENT_SECRET"
  "TWITCH_REDIRECT_URL"
  "MYSQL_ROOT_PASSWORD"
  "MYSQL_PASSWORD"
  "MYSQL_DATABASE"
  "MYSQL_USER"
  "APP_SESSION_SECRET"
  "ACME_EMAIL"
  "TRAEFIK_AUTH"
)

MISSING_VARS=()

for var in "${REQUIRED_VARS[@]}"; do
  if [ -z "${!var}" ]; then
    MISSING_VARS+=("$var")
  fi
done

if [ ${#MISSING_VARS[@]} -gt 0 ]; then
  echo "❌ Variables manquantes dans .env.$APP_ENV :"
  for var in "${MISSING_VARS[@]}"; do
    echo "  - $var"
  done
  echo ""
  echo "📝 Actions possibles :"
  echo "  1. Copier .env.example vers .env.$APP_ENV"
  echo "  2. Générer les secrets: mise run env-generate"
  echo "  3. Remplir les valeurs manquantes"
  exit 1
fi

echo "✅ Toutes les variables d'environnement requises sont définies"
echo "🌐 Environnement: $APP_ENV"
echo "🔗 Redirect URL: $TWITCH_REDIRECT_URL"
echo "📊 Monitoring: $ENABLE_MONITORING"
"""

[tasks.env-generate]
description = "Générer les secrets pour .env"
run = """
#!/usr/bin/env bash
set -e

echo "# ======================================"
echo "# Secrets générés automatiquement"
echo "# ======================================"
echo ""

echo "# MySQL Root Password"
echo "MYSQL_ROOT_PASSWORD=$(openssl rand -base64 32)"
echo ""

echo "# MySQL User Password"
echo "MYSQL_PASSWORD=$(openssl rand -base64 32)"
echo ""

echo "# App Session Secret"
echo "APP_SESSION_SECRET=$(openssl rand -base64 32)"
echo ""

if command -v htpasswd &> /dev/null; then
  echo "# Traefik Auth (admin/changeme)"
  echo "# Générer un nouveau mot de passe : htpasswd -nB admin"
  echo "TRAEFIK_AUTH=$(echo $(htpasswd -nB admin 2>/dev/null) | sed -e 's/\$/\$\$/g')"
else
  echo "# Traefik Auth (installer apache2-utils pour générer)"
  echo "# TRAEFIK_AUTH=admin:\$\$2y\$\$05\$\$..."
fi

echo ""
echo "📝 Copier ces valeurs dans .env.development et .env.production"
echo "🔒 Utiliser des secrets différents pour dev et prod!"
"""

[tasks."env:dev"]
description = "Activer le profil development"
run = """
#!/usr/bin/env bash
echo "🛠️  Activation du profil DEVELOPMENT"
export MISE_ENV=development
mise trust
eval "$(mise activate bash)"
"""

[tasks."env:prod"]
description = "Activer le profil production"
run = """
#!/usr/bin/env bash
echo "🚀 Activation du profil PRODUCTION"
export MISE_ENV=production
mise trust
eval "$(mise activate bash)"
"""

[tasks."env:staging"]
description = "Activer le profil staging"
run = """
#!/usr/bin/env bash
echo "🧪 Activation du profil STAGING"
export MISE_ENV=staging
mise trust
eval "$(mise activate bash)"
"""

[tasks.db-backup]
description = "Backup de la base de données"
run = """
#!/usr/bin/env bash
BACKUP_FILE="backup-$APP_ENV-$(date +%Y%m%d-%H%M%S).sql"
echo "💾 Backup de $APP_ENV vers $BACKUP_FILE"
docker exec twitch-chatters-db mariadb-dump -u root -p"$MYSQL_ROOT_PASSWORD" twitch_chatters > "$BACKUP_FILE"
echo "✅ Backup sauvegardé: $BACKUP_FILE"
"""

[tasks.db-restore]
description = "Restaurer la base de données (argument: fichier)"
run = """
#!/usr/bin/env bash
if [ -z "$1" ]; then
  echo "❌ Usage: mise run db-restore <fichier.sql>"
  exit 1
fi

if [ ! -f "$1" ]; then
  echo "❌ Fichier $1 introuvable"
  exit 1
fi

echo "⚠️  Restauration de $1 vers $APP_ENV"
read -p "Confirmer? (y/N) " -n 1 -r
echo

if [[ $REPLY =~ ^[Yy]$ ]]; then
  docker exec -i twitch-chatters-db mariadb -u root -p"$MYSQL_ROOT_PASSWORD" twitch_chatters < "$1"
  echo "✅ Base de données restaurée"
else
  echo "❌ Annulé"
fi
"""

[tasks.db-console]
description = "Ouvrir la console MariaDB"
run = "docker exec -it twitch-chatters-db mariadb -u root -p\"$MYSQL_ROOT_PASSWORD\" twitch_chatters"

[tasks.redis-console]
description = "Ouvrir la console Redis"
run = "docker exec -it twitch-chatters-redis redis-cli"

[tasks.clean]
description = "Nettoyer les fichiers temporaires"
run = """
#!/usr/bin/env bash
echo "🧹 Nettoyage..."
go clean
rm -f coverage.out
rm -f *.log
echo "✅ Nettoyage terminé"
"""
