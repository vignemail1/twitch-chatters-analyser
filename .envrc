# Configuration mise pour charger automatiquement l'environnement
# Compatible avec mise (https://mise.jdx.dev)
# direnv n'est PAS requis - mise gère l'environnement directement

# Charger les overrides locaux si présents
if [ -f ".envrc.local" ]; then
  source .envrc.local
fi

# Détecter le profil depuis la variable d'environnement ou utiliser development par défaut
export MISE_ENV=${MISE_ENV:-development}

echo "🌐 Environnement mise: $MISE_ENV"
echo "📝 Utiliser 'mise run env:dev' ou 'mise run env:prod' pour changer"
echo "🚀 Démarrer: mise run up"
