# Configuration pour mise (https://mise.jdx.dev)
# mise charge automatiquement les .env, pas besoin de direnv

# Détecter le profil depuis MISE_ENV ou utiliser development par défaut
export MISE_ENV=${MISE_ENV:-development}

# Activer mise automatiquement
if command -v mise &> /dev/null; then
  eval "$(mise activate bash)"
fi

# Message d'information
echo "🌐 Environnement: $MISE_ENV"
echo "🛠️  Outils: mise gère l'environnement, fnox gère les secrets"
echo ""
echo "📝 Actions:"
echo "  - Vérifier config: mise run env-check"
echo "  - Gérer secrets: fnox secrets list"
echo "  - Démarrer: mise run up"
echo ""
